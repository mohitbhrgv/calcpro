# ------------------------------------------------------------------
# ENVIRONMENT CONFIGURATION
# ------------------------------------------------------------------
# This file contains your actual development credentials.
# DO NOT COMMIT THIS FILE TO GIT.
# It is ignored via .gitignore.
#
APP_ENV=development

# ------------------------------------------------------------------
# FRONTEND URLS
# ------------------------------------------------------------------
PUBLIC_FRONTEND_URL=http://localhost:3000
ADMIN_FRONTEND_URL=http://localhost:5173

# ------------------------------------------------------------------
# DATABASE CREDENTIALS - **REPLACE WITH YOUR ACTUAL VALUES**
# ------------------------------------------------------------------
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=calculox_db

# ------------------------------------------------------------------
# SMTP EMAIL SERVER DETAILS - **REPLACE WITH YOUR ACTUAL VALUES**
# ------------------------------------------------------------------
SMTP_HOST=your_smtp_host
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_PORT=2525
SMTP_FROM_EMAIL=no-reply@yourdomain.com
SMTP_FROM_NAME="Calculox Support"

# ------------------------------------------------------------------
# SECURITY & AUTHENTICATION - **REPLACE WITH A NEW RANDOM STRING**
# ------------------------------------------------------------------
# WARNING (DISRUPTIVE BUT SAFE): Changing this will instantly log out all active admin users.
JWT_SECRET=your_jwt_secret_key_here

# ------------------------------------------------------------------
# COOKIE DOMAIN CONFIGURATION
# ------------------------------------------------------------------
# For cross-subdomain support (e.g., frontend on admin.domain.com and API on api.domain.com)
# Set this to your root domain prefixed with a dot (e.g., .calculox.com). 
# Leave empty for localhost or single-domain setups.
COOKIE_DOMAIN=

# ------------------------------------------------------------------
# BRUTE FORCE & ENCRYPTION KEYS
# ------------------------------------------------------------------
# DANGER (DO NOT TOUCH): Changing this once live permanently breaks decryption of all existing database IPs.
# APP_ENCRYPTION_KEY MUST be exactly 32-bytes (Base64 Encoded).
# Generate via CLI: php -r "echo base64_encode(random_bytes(32));"
APP_ENCRYPTION_KEY=your_base64_encoded_32_byte_key_here

# DANGER (DO NOT TOUCH): Changing this once live breaks all existing firewall block matches.
# BLIND_INDEX_KEY is a secure salt for hashing IPs.
# Generate via CLI: php -r "echo bin2hex(random_bytes(32));"
BLIND_INDEX_KEY=your_64_character_hex_string_here

# ------------------------------------------------------------------
# API PUBLIC URL
# ------------------------------------------------------------------
# The full public-facing URL to the root of your backend API folder.
# This is used to construct correct links for assets like uploaded images.
# IMPORTANT: This must end with a trailing slash (/).
API_PUBLIC_URL=http://localhost/calcpro-api/

CRON_SECRET=your_cron_secret_here

# ------------------------------------------------------------------
# CLOUD BACKUP & STORAGE (S3 / CLOUDFLARE R2)
# ------------------------------------------------------------------
# Endpoint URL (e.g., https://<account_id>.r2.cloudflarestorage.com)
S3_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com
# Bucket Name
S3_BUCKET=your_bucket_name
# Region (Use 'auto' for Cloudflare R2, or specific region like 'us-east-1' for AWS S3)
S3_REGION=auto
# Access Key ID
S3_ACCESS_KEY=your_s3_access_key
# Secret Access Key
S3_SECRET_KEY=your_s3_secret_key

# ------------------------------------------------------------------
# BACKUP SECURITY SIGNATURE KEY
# ------------------------------------------------------------------
# Used to generate and verify HMAC signatures for local backup integrity.
# WARNING (DO NOT TOUCH): Changing this once live will invalidate ALL existing
# backup signatures, making previously created backups unrestorable.
# Generate via CLI: php -r "echo bin2hex(random_bytes(32));"
BACKUP_HMAC_SECRET=your_backup_hmac_secret_here
