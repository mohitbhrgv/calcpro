import React from 'react';
import SafeIcon from '@/common/SafeIcon';
import * as FiIcons from 'react-icons/fi';

const { FiImage } = FiIcons;

const SocialPreview = ({ formData }) => {
  // Use OG data if available, otherwise fall back to meta or main data
  const title = formData.ogTitle || formData.metaTitle || formData.title || "Your Post Title";
  const description = formData.ogDescription || formData.metaDescription || formData.excerpt || "Your post description will appear here. Write a compelling summary to encourage clicks.";
  const imageUrl = formData.ogImage || formData.featuredImage;

  return (
    <div className="space-y-4">
      <div>
        <h4 className="text-md font-semibold text-gray-800 dark:text-gray-200 mb-1">
          Social Media Preview
        </h4>
        <p className="text-sm text-gray-500 dark:text-gray-400">
          This is a preview of how your post will look when shared on platforms like Facebook or Twitter.
        </p>
      </div>

      <div className="w-full max-w-lg mx-auto bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg overflow-hidden">
        {/* Image Section */}
        <div className="w-full h-48 bg-gray-100 dark:bg-gray-700 flex items-center justify-center">
          {imageUrl ? (
            <img src={imageUrl} alt="Social Preview" className="w-full h-full object-cover" />
          ) : (
            <div className="text-center text-gray-400">
              <SafeIcon icon={FiImage} className="w-10 h-10 mx-auto mb-2" />
              <p className="text-sm">No Image Set (1200x630px recommended)</p>
            </div>
          )}
        </div>

        {/* Text Content Section */}
        <div className="p-4">
          <p className="text-xs text-gray-500 dark:text-gray-400 uppercase">
            calcpro.com
          </p>
          <h5 className="text-md font-bold text-gray-900 dark:text-white truncate">
            {title}
          </h5>
          <p className="text-sm text-gray-600 dark:text-gray-400 mt-1 line-clamp-2">
            {description}
          </p>
        </div>
      </div>
      
      {/* Input Fields for OG data - we'll connect these later */}
      <div className="space-y-4 pt-4 border-t border-gray-200 dark:border-gray-700">
         <div>
            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                Social Title (og:title)
            </label>
            <input
                type="text"
                name="ogTitle"
                value={formData.ogTitle || ''}
                // onChange={onFormDataChange}
                placeholder="Custom title for social media"
                className="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg read-only:bg-gray-100 dark:read-only:bg-gray-600"
                readOnly // Temporarily read-only until we connect it
            />
        </div>
         <div>
            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                Social Description (og:description)
            </label>
            <textarea
                name="ogDescription"
                value={formData.ogDescription || ''}
                // onChange={onFormDataChange}
                rows={3}
                placeholder="Custom description for social media"
                className="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg read-only:bg-gray-100 dark:read-only:bg-gray-600"
                readOnly // Temporarily read-only
            />
        </div>
      </div>
    </div>
  );
};

export default SocialPreview;