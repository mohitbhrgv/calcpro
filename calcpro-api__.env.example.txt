# ------------------------------------------------------------------
# ENVIRONMENT CONFIGURATION
# ------------------------------------------------------------------
# This file serves as a template for the required environment variables.
# Copy this file to a new file named ".env" and fill in your actual credentials.
# The .env file is excluded from version control for security.
#
# APP_ENV can be 'development' or 'production'
APP_ENV=development

# ------------------------------------------------------------------
# FRONTEND URLS (UPDATED)
# ------------------------------------------------------------------
# The base URL of your PUBLIC-FACING React application.
PUBLIC_FRONTEND_URL=http://localhost:5174

# The base URL of your ADMIN CPANEL React application.
ADMIN_FRONTEND_URL=http://localhost:5173


# ------------------------------------------------------------------
# DATABASE CREDENTIALS
# ------------------------------------------------------------------
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=calcpro_db

# ------------------------------------------------------------------
# SMTP EMAIL SERVER DETAILS (for Mailtrap or other services)
# ------------------------------------------------------------------
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_PORT=2525
SMTP_FROM_EMAIL=no-reply@calcpro.com
SMTP_FROM_NAME="CalcPro Support"

# ------------------------------------------------------------------
# SECURITY & AUTHENTICATION
# ------------------------------------------------------------------
# Generate a long, random string (64+ characters) for this secret.
# You can use an online generator or a command line tool.
JWT_SECRET=your-super-secret-and-long-random-string-goes-here

# ------------------------------------------------------------------
# API PUBLIC URL (NEW)
# ------------------------------------------------------------------
# The full public-facing URL to the root of your backend API folder.
# This is used to construct correct links for assets like uploaded images.
# IMPORTANT: This must end with a trailing slash (/).
API_PUBLIC_URL=http://localhost/calcpro-api/